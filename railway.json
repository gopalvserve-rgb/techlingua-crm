{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd web && npm ci && npm run build && cd .. && rm -rf api/public && mkdir -p api/public && cp -r web/dist/. api/public/ && cd api && npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "cd api && node dist/database/migrate.js && node dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
